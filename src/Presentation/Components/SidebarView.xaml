<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MauiHighFidelityDashboard.Domain.Models"
             x:Class="MauiHighFidelityDashboard.Presentation.Components.SidebarView"
             x:Name="SidebarSelf"
             BackgroundColor="#071A52"
             WidthRequest="220">
    <ScrollView>
        <VerticalStackLayout Padding="0">
            <VerticalStackLayout Padding="20,24,20,20">
                <Label Text="MAUI DASHBOARD"
                       TextColor="White"
                       FontSize="16"
                       FontAttributes="Bold" />
                <BoxView HeightRequest="1"
                         Color="#1A3A6A"
                         Margin="0,12,0,0" />
            </VerticalStackLayout>
            <CollectionView ItemsSource="{Binding MenuItems, Source={x:Reference SidebarSelf}}"
                            SelectionMode="Single"
                            SelectionChanged="OnSelectionChanged">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:MenuItemModel">
                        <Border Padding="20,12"
                                BackgroundColor="Transparent"
                                StrokeThickness="0">
                            <Label FontSize="14"
                                   TextColor="#8A9BB5">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding Icon}" TextColor="White" />
                                        <Span Text=" " />
                                        <Span Text="{Binding Label}" />
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                            <Border.Triggers>
                                <DataTrigger TargetType="Border"
                                             Binding="{Binding IsActive}"
                                             Value="True">
                                    <Setter Property="BackgroundColor" Value="#1A3A6A" />
                                </DataTrigger>
                            </Border.Triggers>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentView>
